export const uploadStart = ( ) => {

}

/**
 * This Funciton is used to upload the part to cloud using the uploadId
 * it will track the part number to make them sequential.
 */
export const uploadPart = ( ) => {
    
}